# Production Server Environment Variables
# Copy this file to .env on your server and fill in your values

# Authentication (GENERATE a secure token!)
LLC_MCP_TOKEN=REPLACE_WITH_SECURE_TOKEN

# API Keys
LLC_NOMIC_API_KEY=your-production-nomic-key
LLC_GOOGLE_API_KEY=your-production-google-key
LLC_OPENROUTER_API_KEY=your-production-openrouter-key

# Graph LLM Configuration
LLC_GRAPH_LLM_ENABLED=true
LLC_GRAPH_LLM_MODEL=qwen/qwen3-235b-a22b-thinking-2507

# Embedding Provider
LLC_EMBEDDER_PROVIDER=nomic

# CORS Origins (Update with your Tailnet hostname)
MCP_CORS_ORIGINS=http://talhas-laboratory.tailefe062.ts.net

# Image tags (pin to immutable tags or digests)
LLC_FRONTEND_IMAGE=ghcr.io/OWNER/REPO-frontend:REPLACE_WITH_TAG_OR_DIGEST
LLC_MCP_IMAGE=ghcr.io/OWNER/REPO-mcp:REPLACE_WITH_TAG_OR_DIGEST
LLC_WORKERS_IMAGE=ghcr.io/OWNER/REPO-workers:REPLACE_WITH_TAG_OR_DIGEST
MINIO_IMAGE=minio/minio:REPLACE_WITH_RELEASE_TAG

# Observability (optional)
GRAFANA_ADMIN_PASSWORD=REPLACE_WITH_STRONG_PASSWORD
